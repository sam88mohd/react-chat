import { Typography } from "@mui/material";
import React from "react";

const Chat = () => {
  return (
    <section>
      <Typography variant="h5">Chat</Typography>
    </section>
  );
};

export default Chat;
